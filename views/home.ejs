<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ambulance Tracking System</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    :root {
      --primary-color: #b71c1c; /* Deep red */
      --secondary-color: #d32f2f;
      --accent-color: #c62828;
      --light-color: #fff8f8;
      --text-color: #2c2c2c;
      --card-bg: #ffffff;
      --primary-blue: #d32f2f; /* Replaced blue with red tone */
      --dark-blue: #b71c1c;
      --light-gray: #fef2f2;
      --medium-gray: #ddd;
      --dark-gray: #5e5e5e;
      --shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
      --transition: all 0.3s ease;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    body {
      background-color: var(--light-gray);
      color: var(--text-color);
      line-height: 1.6;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 30px 20px;
    }

    header {
      text-align: center;
      margin-bottom: 40px;
    }

    .project-title {
      font-size: 2.5rem;
      font-weight: 700;
      color: var(--primary-color);
      margin-bottom: 10px;
    }

    .subject-title {
      font-size: 1.3rem;
      color: var(--accent-color);
      font-weight: 500;
      margin-bottom: 30px;
      display: inline-block;
      padding: 8px 20px;
      background-color: rgba(183, 28, 28, 0.1);
      border-radius: 20px;
    }

    .auth-buttons {
      display: flex;
      gap: 15px;
      justify-content: center;
      margin-top: 30px;
    }

    .btn-primary {
      background: var(--primary-blue);
      color: white;
      padding: 12px 30px;
      border-radius: 8px;
      text-decoration: none;
      transition: var(--transition);
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }

    .btn-secondary {
      background: var(--light-color);
      color: var(--primary-blue);
      border: 2px solid var(--primary-blue);
      padding: 12px 30px;
      border-radius: 8px;
      text-decoration: none;
      transition: var(--transition);
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow);
      background: var(--dark-blue);
    }

    .btn-secondary:hover {
      background: var(--primary-blue);
      color: white;
    }

    .team-section, .motivation-section {
      margin-bottom: 50px;
    }

    .section-title {
      font-size: 1.8rem;
      color: var(--primary-color);
      margin-bottom: 25px;
      position: relative;
      padding-bottom: 10px;
    }

    .section-title::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 60px;
      height: 3px;
      background-color: var(--secondary-color);
    }

    .team-name {
      font-size: 1.4rem;
      color: var(--accent-color);
      margin-bottom: 20px;
      font-weight: 600;
    }

    .team-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 30px;
      margin-top: 20px;
    }

    .member-card {
      background-color: var(--card-bg);
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .member-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.12);
    }

    .member-photo {
      width: 100%;
      height: 220px;
      object-fit: cover;
      border-bottom: 1px solid #eee;
    }

    .member-info {
      padding: 20px;
    }

    .member-name {
      font-size: 1.2rem;
      font-weight: 600;
      color: var(--primary-color);
      margin-bottom: 5px;
    }

    .member-role {
      font-size: 0.85rem;
      color: var(--secondary-color);
      font-weight: 500;
      margin-bottom: 15px;
      display: inline-block;
      background-color: rgba(211, 47, 47, 0.1);
      padding: 3px 10px;
      border-radius: 15px;
    }

    .member-detail {
      font-size: 0.9rem;
      margin-bottom: 8px;
      color: var(--accent-color);
    }

    .member-detail span {
      font-weight: 500;
      color: var(--text-color);
    }

    .motivation-content {
      background-color: var(--card-bg);
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
      line-height: 1.8;
    }

    .motivation-content p {
      margin-bottom: 20px;
      font-size: 1.05rem;
    }

    @media (max-width: 768px) {
      .project-title {
        font-size: 2rem;
      }

      .subject-title {
        font-size: 1.1rem;
      }

      .section-title {
        font-size: 1.5rem;
      }

      .team-grid {
        grid-template-columns: 1fr;
      }

      .auth-buttons {
        flex-direction: column;
        align-items: center;
      }

      .btn-primary, .btn-secondary {
        width: 100%;
        text-align: center;
      }
    }

    @media (max-width: 480px) {
      .project-title {
        font-size: 1.8rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1 class="project-title">Ambulance Tracking and Emergency Routing</h1>
      <div class="subject-title">Project Based Learning DAA</div>
    </header>
    <div class="auth-buttons">
      <a href="/map" class="btn-primary">
        <i class="fas fa-ambulance"></i> Track Ambulance
      </a>
      <a href="/adminlogin" class="btn-secondary">
        <i class="fas fa-user-shield"></i> Admin Login
      </a>
    </div>

    <section class="team-section">
      <h2 class="section-title">Team Information</h2>
      <div class="team-name">The Life Saviours</div>
      <div class="team-grid">
        <!-- Team Members -->
        <!-- Keep member cards unchanged -->
        <!-- ... (same member card HTML as before) -->
      </div>
    </section>
  </div>
</body>
</html>
